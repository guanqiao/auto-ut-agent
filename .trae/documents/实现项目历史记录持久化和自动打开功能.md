## 目标
实现项目历史记录的持久化和自动打开上次项目的功能。

## 分析结果
1. 现有配置系统已经很完善（[config.py](file:///d:/opensource/github/auto-ut-agent/pyutagent/core/config.py)），支持 LLM、Aider 和应用配置的保存/加载
2. Aider 高级配置可以正确持久化
3. 缺少项目历史记录功能

## 实现方案

### 1. 扩展配置系统 ([config.py](file:///d:/opensource/github/auto-ut-agent/pyutagent/core/config.py))
- 添加 `ProjectHistory` 数据类，包含项目路径和最后打开时间
- 添加 `AppState` 数据类，包含最近项目列表和最后打开的项目路径
- 添加 `get_app_state_path()` 函数
- 添加 `save_app_state()` 和 `load_app_state()` 函数

### 2. 修改主窗口 ([main_window.py](file:///d:/opensource/github/auto-ut-agent/pyutagent/ui/main_window.py))
- 在 `__init__` 中加载应用状态
- 在 `on_open_project` 中保存项目历史
- 添加 `load_last_project()` 方法，启动时自动打开上次项目
- 在菜单中添加"最近打开的项目"子菜单

### 3. 修改主入口 ([main.py](file:///d:/opensource/github/auto-ut-agent/pyutagent/main.py))
- 在创建 MainWindow 后调用 `load_last_project()`

## 文件修改清单
1. `pyutagent/core/config.py` - 添加项目历史和应用状态相关类和函数
2. `pyutagent/ui/main_window.py` - 添加项目历史管理和自动加载功能
3. `pyutagent/main.py` - 启动时自动加载上次项目

请确认这个方案后，我将开始实施具体的代码修改。