## 问题描述

测试文件导入失败，因为 core 模块缺少测试依赖的类和函数：
- `AsyncRetryWithBackoff` 类
- `create_retry_manager()` 函数

## 修复步骤

### 1. 添加缺失的类到 core/retry_manager.py

添加 `AsyncRetryWithBackoff` 类和 `create_retry_manager()` 函数

### 2. 更新兼容层导出

更新 `pyutagent/tools/retry_manager.py` 导出这些新类

### 3. 更新测试文件导入

将测试文件改为从 `pyutagent.core` 导入

### 4. 运行测试验证

执行测试确保修复正确